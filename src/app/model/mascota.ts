export interface Mascota {
    /*idMascota: number;  //cambiar idMascota por id
    nombre: string;  //nombreMascota se cambio como nombre para que los datos del backend cargue
    especie: string;
    edad: number;
    usuario_id: number;  // Foreign key
    historialMedico: string; */ // Example field for medical history

    id: number;  //cambiar idMascota por id
    nombre: string;  //nombreMascota se cambio como nombre para que los datos del backend cargue
    especie: string;
    raza: number;
    edad: number;
  }
  